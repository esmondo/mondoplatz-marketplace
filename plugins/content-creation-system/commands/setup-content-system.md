# Setup Content Creation System

For full setup with Python setup wizard and config:

```bash
curl -fsSL https://raw.githubusercontent.com/esmondo/berkarja/main/scripts/install.sh | bash
```

Then run the setup wizard:

```bash
cd ~/.berkarja/content-creation-system && python sub-agents/setup-wizard/interview.py
```
